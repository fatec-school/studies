services:
  keycloak:
    container_name: course_keycloak
    image: quay.io/keycloak/keycloak:21.1.1
    ports:
      - 9000:8080
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    command: start-dev
