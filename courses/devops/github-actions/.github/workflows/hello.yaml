name: Hello Github

on:
    push:
        branches:
            - main

jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Show message
              run: echo "You're using github actions! Congrats"
