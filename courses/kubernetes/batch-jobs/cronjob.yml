apiVersion: batch/v1
kind: CronJob
metadata:
  name: echo-command
spec:
  schedule: "* * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: myjob
              image: alpine
              command:
                - echo
                - "aula"
          restartPolicy: OnFailure
---
