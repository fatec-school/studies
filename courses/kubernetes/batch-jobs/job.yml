apiVersion: batch/v1
kind: Job
metadata:
  name: echo-command
spec:
  template:
    spec:
      containers:
        - name: myjob
          image: alpine
          command:
            - echo
            - "aula"
      restartPolicy: OnFailure
  ttlSecondsAfterFinished: 0
---
